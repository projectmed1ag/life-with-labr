# Puppy care overview

Mode: Read. Scope: the care section on `/puppies/` and `/en/puppies/`.

The heading leads into three equally important topics: documents and vaccinations, meeting a puppy, and support after moving home. Preserve the existing factual copy and the public cream, forest and serif styling.

Use one heading above three unboxed columns with fine top rules. Align this section to the catalogue's 1180px content width; do not reserve a separate half-width column for the heading. The heading and columns are separated by 40px. Articles have 24px top padding and no fixed heights. Paragraphs use the existing dark muted green `#5b665b` for readable contrast.

At 900px and below, use a single linear list with 28px between articles. On phones the heading-to-list gap also becomes 28px. Allow natural heading wrapping and preserve DOM reading order. Changes here are scoped to `.puppies-page`, leaving the homepage care section independent.
